const fieldMapper = (asset: any, documentProps: any): any => {
  // Do custom mapping of fields here. Example:
  // asset.altText = { nb: documentProps.description }

  return asset
}

export default fieldMapper
